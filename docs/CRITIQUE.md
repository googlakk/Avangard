# Критика Design System (от лица Senior Frontend Dev)

Привет! Я проанализировал текущий `DESIGN_SYSTEM.md` и конфигурацию проекта. Документ хороший для старта, но если мы хотим **масштабируемую** разработку с участием AI-агентов, в нем зияют опасные дыры.

Вот мой разбор по пунктам:

### 🚨 1. Отсутствие состояний (States)
В документе описан "счастливый путь" (default state). Но интерфейс живой.
- **Где Hover/Focus для карточек?** Агент сам придумает обводку или тень? Скорее всего, кривую.
- **Где Disabled state для кнопок?** Что если форма невалидна? Агент сделает кнопку просто серой (`bg-gray-400`) или полупрозрачной (`opacity-50`)? Это нужно стандартизировать.
- **Где Error state для инпутов?** Красная обводка? Текст ошибки снизу? Этого нет.

### 🎨 2. Типографика: "На глаз"
- Указаны шрифты, но нет системы размеров (Type Scale).
- Агент будет ставить `text-3xl` там, где нужно `text-2xl`, и `font-bold` там, где в дизайне `font-semibold`.
- **Решение:** Ввести семантические классы: `.h1-hero`, `.h2-section`, `.body-lead`.

### 🧹 3. Цветовой хаос (потенциальный)
- В `tailwind.config.ts` есть шикарная палитра `navy-50`...`navy-950`.
- В доке сказано только про `navy-900` и `gray-50`.
- **Риск:** Агент начнет использовать `navy-500` для фона кнопок, потому что "ему так показалось", и это сломает премиальный вид. Нужно четко прописать: "Navy-500 запрещен для фонов, используем только 800/900".

### 🧩 4. Иконки
- "Используй SVG". Откуда? Heroicons? Lucide? FontAwesome?
- Если агент возьмет иконку с `stroke-width=2`, а рядом будет `1.5`, дизайн будет выглядеть "грязным".
- Нужно жестко зафиксировать библиотеку (рекомендую `lucide-react` или `heroicons`) и толщину линий.

### 📱 5. Адаптивность (Mobile)
- "Mobile first" — это лозунг.
- Нужны конкретные правила: "На мобиле отступы секций `py-8`, на десктопе `py-20`". "На мобиле шрифт заголовка `text-2xl`, на десктопе `text-4xl`".

---

## 🚀 Предложение по улучшению

Я предлагаю переписать `docs/DESIGN_SYSTEM.md`, превратив его из "описания" в "спецификацию".

**Что я добавлю:**
1.  **Interactive States:** Четкие классы для hover, focus-visible, disabled.
2.  **Form Elements Specs:** Полный стиль для Input, Checkbox, Textarea (включая состояния ошибки).
3.  **Spacing System:** Правила отступов для секций vs компонентов.
4.  **Icon Guidelines:** Библиотека + размер + толщина штриха.
5.  **Type Scale Table:** Таблица соответствия: H1 = font-serif + bold + text-4xl.

**Даешь добро на апгрейд?**
